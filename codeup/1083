#include <stdio.h>

int main(void)
{
	int num1;
	int a;
	scanf("%d", &a);

	for (num1 = 1; num1 <= a; num1++)
	{
		if (num1 % 3 == 0)
		{
			printf("X ");
		}
		else
		{
			printf("%d ", num1);
		}
	}

	return 0;
}
