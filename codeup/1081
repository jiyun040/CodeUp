#include <stdio.h>

int main(void)
{
	int n, m;
	scanf("%d %d", &n, &m);
	for (int i = 1; i <= n; i++) //i가 n보다 작거나 같을 때 까지 i를 증가시켜줌 
	{
		for (int j = 1; j <= m; j++) //j가 m보다 작거나 같을 때 까지 j를 증가시켜줌
		{
			printf("%d %d\n", i, j); //i와 j를 증가시켜주면서 나온 값들을 출력 
		}
	}


	return 0;
}
