#include <stdio.h>

int main(void)
{
	int num, i;
	int	sum = 0;
	scanf("%d", &num);
	for (i = 1; i <= num; i++)
	{
		if (i % 2 == 0)
		{
			sum += i;
		}
	}
	printf("%d", sum); //printf를 for문 밖으로 꺼내줘야 함 
		return 0;
	}
